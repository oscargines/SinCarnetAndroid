<?xml version="1.0" encoding="utf-8"?>
<!--
    Network Security Configuration - SinCarnet
    Configuración de seguridad para tráfico de red.
    Por defecto, solo permite HTTPS (cleartext deshabilitado).
-->
<network-security-config>
    <!-- Por defecto: solo HTTPS -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!--
    Si necesitas permitir tráfico HTTP a dominios específicos (solo debug), descomenta:
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">localhost</domain>
    </domain-config>
    -->
</network-security-config>
